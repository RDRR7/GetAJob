<nav style="padding: 1rem 1rem;">
    <% if current_company.nil? and current_user.nil?%>
        <a href=<%= new_company_session_path %> class="button button_gray button_small">Sign in</a>
        <a href=<%= new_company_registration_path %> class="button button_gray button_small">Sign up</a>
    <% elsif current_user.nil? %>
        <a href=<%= destroy_company_session_path %> data-method="delete" class="button button_gray button_small">Log out</a>
    <% end %>
</nav>